#include <stdio.h>

int main()
{
    int a = 0;
    int b = 1;
    scanf("%d", &a);
    scanf("%d", &b);
    if (a > b)
    {
        printf("%d", a);
    }
    else
    {
        printf("%d", b);
    }
    return 0;
}
